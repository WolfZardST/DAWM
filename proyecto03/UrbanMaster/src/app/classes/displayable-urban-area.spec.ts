import { DisplayableUrbanArea } from './displayable-urban-area';

describe('DisplayableUrbanArea', () => {
  it('should create an instance', () => {
    expect(new DisplayableUrbanArea()).toBeTruthy();
  });
});
